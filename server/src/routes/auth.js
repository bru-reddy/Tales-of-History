import {Router}from"express";import bcrypt from"bcryptjs";import {User}from"../models.js";import {signUser,auth}from"../auth.js";
const r=Router();
const cleanEmail=e=>String(e||"").trim().toLowerCase();
r.post("/register",async(req,res)=>{try{const name=String(req.body?.name||"").trim(),email=cleanEmail(req.body?.email),password=String(req.body?.password||"");if(!name||!email||!password)return res.status(400).json({message:"Name, email and password are required"});if(!/^\S+@\S+\.\S+$/.test(email))return res.status(400).json({message:"Please enter a valid email address"});if(password.length<6)return res.status(400).json({message:"Password must be at least 6 characters"});if(await User.findOne({email}))return res.status(409).json({message:"An account with this email already exists. Sign in instead."});const u=await User.create({name,email,passwordHash:await bcrypt.hash(password,12)});res.status(201).json({token:signUser(u),user:{id:u._id,name:u.name,email:u.email}})}catch(e){if(e?.code===11000)return res.status(409).json({message:"An account with this email already exists. Sign in instead."});res.status(500).json({message:"Registration failed. Please try again."})}});
r.post("/login",async(req,res)=>{try{const email=cleanEmail(req.body?.email),password=String(req.body?.password||"");if(!email||!password)return res.status(400).json({message:"Email and password are required"});const u=await User.findOne({email});if(!u||!(await bcrypt.compare(password,u.passwordHash)))return res.status(401).json({message:"Invalid email or password"});res.json({token:signUser(u),user:{id:u._id,name:u.name,email:u.email}})}catch{res.status(500).json({message:"Sign in failed. Please try again."})}});
r.get("/me",auth,async(req,res)=>{const u=await User.findById(req.user.id).select("_id name email interests completedTopics");if(!u)return res.status(404).json({message:"User not found"});res.json({user:{id:u._id,name:u.name,email:u.email,interests:u.interests,completedTopics:u.completedTopics}})});
export default r;