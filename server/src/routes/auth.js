import {Router} from "express";import bcrypt from "bcryptjs";import {User} from "../models.js";import {signUser} from "../auth.js";
const r=Router();
r.post("/register",async(req,res)=>{try{const{name,email,password}=req.body;if(!name||!email||!password)return res.status(400).json({message:"Name, email and password are required"});if(password.length<6)return res.status(400).json({message:"Password must be at least 6 characters"});if(await User.findOne({email:email.toLowerCase()}))return res.status(409).json({message:"Account already exists"});const u=await User.create({name,email,passwordHash:await bcrypt.hash(password,12)});res.status(201).json({token:signUser(u),user:{id:u._id,name:u.name,email:u.email}})}catch{res.status(500).json({message:"Registration failed"})}});
r.post("/login",async(req,res)=>{const u=await User.findOne({email:req.body.email?.toLowerCase()});if(!u||!(await bcrypt.compare(req.body.password||"",u.passwordHash)))return res.status(401).json({message:"Invalid email or password"});res.json({token:signUser(u),user:{id:u._id,name:u.name,email:u.email}})});
r.get("/me",async(req,res)=>res.json({message:"Authentication endpoint available"}));export default r;